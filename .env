# Local environment file
#
# Never commit credentials or deployment secrets to this repository.
# Create this file locally from sample.env and keep it untracked:
#
#   Copy-Item sample.env .env
#
# Store production values in the deployment platform's secret/environment
# configuration. See sample.env for supported variables.
